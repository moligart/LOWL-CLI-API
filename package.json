{
  "name": "cli-test-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "mocha --timeout 10000 --exit",
    "test-dev": "NODE_ENV=dev && mocha --timeout 10000 --exit",
    "start-dev": "NODE_ENV=dev && node server.js",
    "start": "NODE_ENV=default && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.1",
    "body-parser": "^1.18.2",
    "config": "^1.30.0",
    "express": "^4.16.2",
    "mongoose": "^4.13.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-airbnb": "^2.4.0",
    "chai": "^4.1.2",
    "chai-http": "^3.0.0",
    "mocha": "^5.0.4"
  }
}
